[package]
name = "afmelder-app"
edition = "2021"
version = "0.1.1"
description = "A simple web server for generating excuses to skip the daily"
license = "MIT"
resolver = "2"

[dependencies]
actix-web = "4"
rand = "0.8.5"
serde = { version = "1.0", features = ["derive"] }
